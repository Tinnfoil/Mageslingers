using UnityEngine;
using UnityEditor.ShaderGraph;
using System.Reflection;

public class AdditionalLights : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
